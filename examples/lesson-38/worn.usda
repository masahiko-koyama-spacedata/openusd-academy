#usda 1.0
(
    defaultPrim = "Worn"
    metersPerUnit = 1
    upAxis = "Y"
)

# 上からかぶせる小さなLayer。Instance Prim へ参照として足す
over "Worn"
{
    over "Seat"
    {
        color3f[] primvars:displayColor = [(0.45, 0.3, 0.2)] (
            interpolation = "constant"
        )
    }
}
