#usda 1.0
(
    defaultPrim = "Ball"
    metersPerUnit = 1
    upAxis = "Y"
)

# 見た目だけを持つLayer。muteするとこの色が消える
over "Ball"
{
    color3f[] primvars:displayColor = [(0.9, 0.2, 0.2)] (
        interpolation = "constant"
    )
}
